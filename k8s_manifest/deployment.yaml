apiVersion: apps/v1
kind: Deployment
metadata:
  name: react
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react
  template:
    metadata:
      labels:
        app: react
    spec:
      containers:
      - name: react
        image: arshad200/react_with_node:latest
        ports:
        - containerPort: 3000
        resources:
          requests:
            cpu: "10m"
